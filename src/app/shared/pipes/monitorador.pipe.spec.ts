import { MonitoradorPipe } from './monitorador.pipe';

describe('MonitoradorPipe', () => {
  it('create an instance', () => {
    const pipe = new MonitoradorPipe();
    expect(pipe).toBeTruthy();
  });
});
